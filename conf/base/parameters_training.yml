training: #see globals.yml for adjustment of n_qubits, n_layers,...
  n_qubits: 4
  n_layers: 14
  vqc_ansatz: "Circuit_19" 
  data_reupload: 1.0
  learning_rate: 0.07
  shots: "None"
  optimizer: "Adam"
  steps: 400
  report_figure_every_n_steps: 50
  loss: "mse" #mse, fft_ssim, ssim, psnr
  output_qubit: 0 # -1 to use all qubits or a specific qubit number
  initialization: "random"
  seed: 1111
